# Comparative Analysis Seed — v0.6.0

## Purpose

This file prepares the later v0.7.0 comparative layer.

It identifies the main neighboring frameworks and the question that must be answered:

    What does the Law of Totality add?

## Neighboring Fields

### Model Risk Management

Already contains:

    model assumptions
    validation
    model limitations
    use monitoring
    governance

Law of Totality adds:

    a general cross-domain error formula connecting local closure, actual use, valid scope, and excluded critical dependency.

### NIST AI Risk Management Framework

Already contains:

    context of use
    AI risk
    governance
    mapping
    measuring
    managing

Law of Totality adds:

    a compact structural criterion for when an AI output becomes invalid as a finite treatment used beyond scope.

### STPA / STAMP

Already contains:

    unsafe control actions
    constraints
    feedback
    process models
    system-level hazards

Law of Totality adds:

    a generalization beyond safety engineering to every finite framework-use relation.

### Map-Territory Distinction

Already contains:

    the map is not the territory

Law of Totality adds:

    an audit formula for when the map becomes structurally invalid for a concrete use.

### Systems Thinking

Already contains:

    interdependence
    emergence
    feedback
    non-isolation

Law of Totality adds:

    a precise error signature: false sufficiency of a local closure under use beyond scope.

## v0.7.0 Target

The next release should include a table with columns:

    field
    what it already captures
    what it does not formalize
    how LOT overlaps
    how LOT differs
    operational added value
